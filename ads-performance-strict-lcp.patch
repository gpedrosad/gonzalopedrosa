diff --git a/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx b/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx
index 298e02a..fa04bb9 100644
--- a/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx
+++ b/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx
@@ -1,6 +1,7 @@
 "use client";
 
 import dynamic from "next/dynamic";
+import { useEffect, useState } from "react";
 
 const StickyWhatsAppCTA = dynamic(
   () => import("./StickyWhatsAppCTA").then((mod) => mod.StickyWhatsAppCTA),
@@ -8,5 +9,25 @@ const StickyWhatsAppCTA = dynamic(
 );
 
 export function LazyStickyWhatsAppCTA({ href }: { href: string }) {
+  const [canRender, setCanRender] = useState(false);
+
+  useEffect(() => {
+    if ("requestIdleCallback" in globalThis) {
+      const id = globalThis.requestIdleCallback(
+        () => setCanRender(true),
+        { timeout: 2200 }
+      );
+
+      return () => globalThis.cancelIdleCallback(id);
+    }
+
+    const timeoutId = setTimeout(() => setCanRender(true), 1200);
+    return () => clearTimeout(timeoutId);
+  }, []);
+
+  if (!canRender) {
+    return null;
+  }
+
   return <StickyWhatsAppCTA href={href} />;
 }
diff --git a/src/app/ads/layout.tsx b/src/app/ads/layout.tsx
index fa97994..40e39a5 100644
--- a/src/app/ads/layout.tsx
+++ b/src/app/ads/layout.tsx
@@ -1,5 +1,6 @@
 import type { Metadata } from "next";
 import Script from "next/script";
+import "./styles.css";
 
 /**
  * Layout específico para landing pages de Google Ads.
@@ -23,13 +24,13 @@ export default function AdsLayout({
 }) {
   return (
     <>
-      {children}
+      <div className="ads-lcp-root">{children}</div>
 
       {/*
         Delegación global para tracking de clics WhatsApp en /ads.
         Evita hidratar cada botón individualmente y conserva eventCallback/eventTimeout.
       */}
-      <Script id="ads-whatsapp-tracking" strategy="afterInteractive">
+      <Script id="ads-whatsapp-tracking" strategy="lazyOnload">
         {`
           (function () {
             if (window.__adsWhatsappTrackingInit) return;
diff --git a/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx b/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx
index 6ba00e2..cbdf612 100644
--- a/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx
+++ b/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx
@@ -1,6 +1,7 @@
 "use client";
 
 import dynamic from "next/dynamic";
+import { useEffect, useState } from "react";
 
 const StickyWhatsAppAfterFirstCta = dynamic(
   () =>
@@ -19,5 +20,25 @@ export function LazyStickyWhatsAppAfterFirstCta({
   href,
   firstCtaId,
 }: LazyStickyWhatsAppAfterFirstCtaProps) {
+  const [canRender, setCanRender] = useState(false);
+
+  useEffect(() => {
+    if ("requestIdleCallback" in globalThis) {
+      const id = globalThis.requestIdleCallback(
+        () => setCanRender(true),
+        { timeout: 2200 }
+      );
+
+      return () => globalThis.cancelIdleCallback(id);
+    }
+
+    const timeoutId = setTimeout(() => setCanRender(true), 1200);
+    return () => clearTimeout(timeoutId);
+  }, []);
+
+  if (!canRender) {
+    return null;
+  }
+
   return <StickyWhatsAppAfterFirstCta href={href} firstCtaId={firstCtaId} />;
 }
diff --git a/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx b/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx
new file mode 100644
index 0000000..fa04bb9
--- /dev/null
+++ b/src/app/ads/compin/LazyStickyWhatsAppCTA.tsx
@@ -0,0 +1,33 @@
+"use client";
+
+import dynamic from "next/dynamic";
+import { useEffect, useState } from "react";
+
+const StickyWhatsAppCTA = dynamic(
+  () => import("./StickyWhatsAppCTA").then((mod) => mod.StickyWhatsAppCTA),
+  { ssr: false }
+);
+
+export function LazyStickyWhatsAppCTA({ href }: { href: string }) {
+  const [canRender, setCanRender] = useState(false);
+
+  useEffect(() => {
+    if ("requestIdleCallback" in globalThis) {
+      const id = globalThis.requestIdleCallback(
+        () => setCanRender(true),
+        { timeout: 2200 }
+      );
+
+      return () => globalThis.cancelIdleCallback(id);
+    }
+
+    const timeoutId = setTimeout(() => setCanRender(true), 1200);
+    return () => clearTimeout(timeoutId);
+  }, []);
+
+  if (!canRender) {
+    return null;
+  }
+
+  return <StickyWhatsAppCTA href={href} />;
+}
diff --git a/src/app/ads/components/AdsProfileImage.tsx b/src/app/ads/components/AdsProfileImage.tsx
new file mode 100644
index 0000000..7e50156
--- /dev/null
+++ b/src/app/ads/components/AdsProfileImage.tsx
@@ -0,0 +1,33 @@
+import Image from "next/image";
+
+type AdsProfileImageProps = {
+  alt: string;
+  width: number;
+  height: number;
+  className?: string;
+  priority?: boolean;
+  sizes?: string;
+};
+
+export function AdsProfileImage({
+  alt,
+  width,
+  height,
+  className,
+  priority = false,
+  sizes,
+}: AdsProfileImageProps) {
+  return (
+    <Image
+      src="/yo.png"
+      alt={alt}
+      width={width}
+      height={height}
+      className={className}
+      priority={priority}
+      loading={priority ? undefined : "lazy"}
+      fetchPriority={priority ? "high" : undefined}
+      sizes={sizes ?? `${width}px`}
+    />
+  );
+}
diff --git a/src/app/ads/components/AdsWhatsAppButton.tsx b/src/app/ads/components/AdsWhatsAppButton.tsx
new file mode 100644
index 0000000..d1e195d
--- /dev/null
+++ b/src/app/ads/components/AdsWhatsAppButton.tsx
@@ -0,0 +1,34 @@
+type AdsWhatsAppButtonProps = {
+  href: string;
+  children: React.ReactNode;
+  className?: string;
+  label?: string;
+};
+
+export function AdsWhatsAppButton({
+  href,
+  children,
+  className = "",
+  label,
+}: AdsWhatsAppButtonProps) {
+  return (
+    <a
+      href={href}
+      target="_blank"
+      rel="noopener noreferrer"
+      data-wa-track="1"
+      data-wa-label={label ?? href}
+      className={`inline-flex items-center justify-center gap-2 px-6 py-3 text-sm font-medium rounded-full cursor-pointer transition-all duration-150 ${className}`}
+    >
+      <svg
+        className="w-5 h-5"
+        viewBox="0 0 24 24"
+        fill="currentColor"
+        aria-hidden="true"
+      >
+        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
+      </svg>
+      {children}
+    </a>
+  );
+}
diff --git a/src/app/ads/styles.css b/src/app/ads/styles.css
new file mode 100644
index 0000000..10243b5
--- /dev/null
+++ b/src/app/ads/styles.css
@@ -0,0 +1,8 @@
+.ads-lcp-root main > section:not(:first-of-type) {
+  content-visibility: auto;
+  contain-intrinsic-size: 1px 920px;
+}
+
+.ads-lcp-root .animate-pulse {
+  animation: none;
+}
diff --git a/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx b/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx
new file mode 100644
index 0000000..cbdf612
--- /dev/null
+++ b/src/app/ads/terapia-pareja/LazyStickyWhatsAppAfterFirstCta.tsx
@@ -0,0 +1,44 @@
+"use client";
+
+import dynamic from "next/dynamic";
+import { useEffect, useState } from "react";
+
+const StickyWhatsAppAfterFirstCta = dynamic(
+  () =>
+    import("./StickyWhatsAppAfterFirstCta").then(
+      (mod) => mod.StickyWhatsAppAfterFirstCta
+    ),
+  { ssr: false }
+);
+
+type LazyStickyWhatsAppAfterFirstCtaProps = {
+  href: string;
+  firstCtaId: string;
+};
+
+export function LazyStickyWhatsAppAfterFirstCta({
+  href,
+  firstCtaId,
+}: LazyStickyWhatsAppAfterFirstCtaProps) {
+  const [canRender, setCanRender] = useState(false);
+
+  useEffect(() => {
+    if ("requestIdleCallback" in globalThis) {
+      const id = globalThis.requestIdleCallback(
+        () => setCanRender(true),
+        { timeout: 2200 }
+      );
+
+      return () => globalThis.cancelIdleCallback(id);
+    }
+
+    const timeoutId = setTimeout(() => setCanRender(true), 1200);
+    return () => clearTimeout(timeoutId);
+  }, []);
+
+  if (!canRender) {
+    return null;
+  }
+
+  return <StickyWhatsAppAfterFirstCta href={href} firstCtaId={firstCtaId} />;
+}
